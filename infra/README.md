# Infrastructure Placeholder

Store reusable infrastructure modules, templates, and deployment scripts here.

Suggested structure:

- `modules/` shared IaC modules
- `params/` environment parameter files
- `scripts/` validation and deployment automation

Suggested checks:

- Static linting
- Security scan
- Deployment validation (dry-run)
- Policy compliance check
